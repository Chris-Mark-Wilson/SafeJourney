import { View, Text } from "react-native"
import { appStyle } from "../styles/appStyle"

export const Home = () => {
    return (
        <View style={appStyle.container}>
            <Text>Map</Text>
        </View>
    )
}